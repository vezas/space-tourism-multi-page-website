import { FC } from 'react';
import styles from 'src/pages/Destination/Destination.module.scss';

export const Destination: FC = () => {
  return (
    <>
      <section className={styles.destination}>Destination</section>
    </>
  );
};
